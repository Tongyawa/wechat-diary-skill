# ---- Test artifacts (per-run outputs, archived by convention) ----
tests/_artifacts/

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---- Editors / OS ----
.vscode/
.idea/
.DS_Store
Thumbs.db

# ---- Local config (contains personal wxids) ----
config.toml
.export-state.json
.session-rename-state.json

# ---- Local engineering notes (kept on disk, not pushed) ----
CLAUDE.md

# ---- Claude Code per-machine settings (machine-specific allow lists) ----
.claude/settings.local.json

# ---- Claude Code agent worktrees (transient, machine-local) ----
.claude/worktrees/
.claude/hand[o]ffs/
.claude/scheduled_tasks.lock

# ---- Raw / intermediate / output data ----
.runlog/
# Suffixed variants (WeFlow-raw-exports-20260607/, WeFlow-raw-exports-去年/ …)
# are backfill snapshots and must stay out of git just like the live roots.
WeFlow-raw-exports*/
WeFlow-processed-exports*/
WeFlow-archived-exports*/
WeFlow-insights*/

# ---- Legacy nested skills stay local; the public skill now lives at repo root ----
.claude/skills/

# ---- Legacy / archival workspace ----
工程/

# ---- Plan files / scratch ----
*.local.md
